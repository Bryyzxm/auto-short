[phases.setup]
aptPkgs = ["python3", "python3-pip", "ffmpeg", "curl"]

[phases.install]
cmds = [
    "pip3 install yt-dlp",
    "cd api && npm ci --only=production"
]

[start]
cmd = "cd api && npm start"

[variables]
NODE_ENV = "production"